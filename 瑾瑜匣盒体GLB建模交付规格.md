# 瑾瑜匣盒体 GLB 建模交付规格

这不是普通木盒。目标是：用户先看到一只合上的真实紫檀百什件匣；点击后，匣盖开启、托盘像抽屉一样向前拉出，镜头转成俯视，再能点每一个格子里的物件。

## 建模参考

- 原型：清乾隆“瑾瑜匣”百什件盒。
- 真实比例：长 55.0 厘米，宽 34.2 厘米，高约 45.5 厘米。
- 参考文件夹：[官方图](/Users/7upradday/Documents/Codex/2026-08-20/web-web-web/outputs/瑾瑜匣调研/官方参考图_高清)。
- 注意：必须做成**横向长方体**，不是竖向柜子，也不是现代抽屉柜。

## 必须拆成的对象节点

| 节点名建议 | 内容 | 是否需要动画 |
| --- | --- | --- |
| `box_outer` | 紫檀外匣、铜饰、提梁 | 否 |
| `lid` | 匣盖／正面开合门 | 是：`open_lid` |
| `tray_main` | 承载百宝格的主托盘 | 是：`pull_tray` |
| `divider_grid` | 横向内格、木质隔板 | 跟随托盘 |
| `slot_01` 至 `slot_20` | 每个可放物件的空位定位点 | 否 |
| `metal_fittings` | 铰链、锁牌、铜件 | 否 |

`slot_01…slot_20` 可以是空节点，不需要做真的凹槽模型；它们的作用是让我在网页里把每件文物模型准确放进格子。

## 两段动画

1. `open_lid`：匣盖/门由闭合转为打开，0.8–1.2 秒。
2. `pull_tray`：主托盘向前平移约盒体深度的 35%–50%，0.7–1.0 秒。

两段动画应可依次播放：先开盖，再拉出托盘。不要用布料、粒子或复杂特效；网页端需要稳定。

## 材质与风格

- 紫檀木：深红棕，细密直纹，有轻微旧感；避免塑料感和过强镜面反射。
- 金属：暗金铜色，带氧化感；不做亮黄色。
- 内部隔板：比外框略浅，但仍是木质。
- 不需要把文物直接烘焙进盒子；盒内应保持空位，网页再加载文物模型。

## 网页性能交付

- 格式：GLB（glTF 2.0）。
- 贴图：尽量使用 WebP/KTX2；单张不超过 2048×2048。
- 目标体积：建议 **8–15MB**，上限 20MB。
- 提交时另附：正面、左 45°、右 45°、俯视四张 PNG 预览图。
- 坐标：盒体正面朝 +Z，顶部朝 +Y，中心落在原点；单位使用米。

## 不要做

- 不要把隔间做成平均九宫格。
- 不要把文物和盒体烘焙成一张静态模型。
- 不要只给闭合状态。
- 不要用“把整个盒子旋转起来”代替抽屉拉出动画。

交付后把 GLB 与四张预览图发给我；我会将它接入开匣流程，并用 `slot_01…slot_20` 放置 2.5D/3D 文物。

## 小红书发布版的转换规则

小红书小工具不能直接打包或加载 GLB，也不能调用外部 CDN 的 3D 查看器。因此盒体 GLB 是“母版资产”：我会从中输出合匣、开盖、托盘拉出、俯视展开等关键帧，做成内置 WebP/PNG 图序列和动画。

这仍然能实现真实器物外观、开盖和抽屉拉出；但发布版不是实时 WebGL。若需要完整自由旋转的实时 3D，需要另做普通网站版，不和小红书小工具版混用。
