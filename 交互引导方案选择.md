# 乾隆玩具盒｜玩法引导交互选择

## 1. “模型没有丢失”提示排查

这不是 GLB 文件损坏，也不是模型加载逻辑误报。

- 该提示只在 `window.location.protocol === "file:"` 时出现。
- 即用户直接双击 `Build/index.html`，地址栏显示“文件”或 `file://`。
- 浏览器在该模式下会限制页面读取本地 GLB 与内嵌贴图，因此项目主动遮住页面，避免出现无贴图或黑屏模型。
- 使用 `http://127.0.0.1:4173/` 打开时，提示不出现，模型资源正常加载。

结论：这是正常保护机制，不需要修模型。后续可以把提示标题改成“当前是文件预览模式”，减少误解。

## 2. 当前问题

“轻点看物 · 在卡片里替换 · 长按拖动隔间”是核心玩法，却位于柜体下方并使用弱提示样式。用户视线优先落在 3D 柜体和开匣按钮上，容易跳过说明，尤其难以发现“长按移动”。

本轮只建立两套独立预览，不修改正式入口。选定后再合并至主版本。

## 3. 方案 A：御匣三诀（推荐首发）

### 结构

乾隆对话 → 主柜 → 柜体上方“御匣三诀” → 开匣。

三个操作并列：

1. 轻点看物：打开珍玩档案。
2. 卡片替换：点开卡片后，从宝库重新钦点。
3. 长按移格：重排皇家陈列。

### 优点

- 不新增流程页，保持“对话结束马上见柜”的期待。
- 三项操作获得稳定、同等的视觉权重。
- 开匣后仍能随时回看，适合第一次没记住的用户。
- 实现与维护成本低，对首版节奏影响最小。

### 风险

- 沉浸感弱于剧情式教学。
- 需要控制组件高度，避免压缩 3D 柜体的展示面积。

## 4. 方案 B：乾隆带你玩

### 结构

乾隆对话 → 三张右滑操作谕旨 → CTA“怎么样？想不想亲临朕的百宝盒？” → 主柜。

三张内容：

1. 乾隆示范轻点，标题“看中哪件，轻点细看”。
2. 乾隆示范替换，标题“不合心意，卡片里换”。
3. 乾隆示范长按，标题“长按不松，重排隔间”。

### 优点

- 乾隆角色存在感强，教程本身成为内容体验。
- 手势、结果和入口被拆开讲，理解最完整。
- CTA 能形成明确的开匣期待。

### 风险

- 用户正式见柜前多出约 8–12 秒。
- 三张 PNG 需要统一人物、手势、柜格和光影，后续素材成本更高。
- 应提供“跳过”或仅首次展示，避免复访用户重复观看。

## 5. 建议

首版选择 A。它直接解决玩法提示不可见的问题，同时不改变已经稳定的开场节奏。若后续更强调小红书传播与角色 IP，再把 B 作为首次进入限定的增强引导。

如果选择 B，正式开发前先完成三张 PNG 的人物分镜确认，再接入滑动与首次展示状态；不要先改主流程再返工素材。
